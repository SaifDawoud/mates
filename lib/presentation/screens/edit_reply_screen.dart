/*
import 'package:flutter/material.dart';
import 'package:mates/models/post_model.dart';
import 'package:mates/models/team_model.dart';
import 'package:mates/providers/post_provider.dart';
import 'package:provider/provider.dart';

class EditReplyScreen extends StatefulWidget {
  String postId;
  String replyId;

  EditReplyScreen(this.postId,this.replyId);

  @override
  _EditReplyScreenState createState() => _EditReplyScreenState();
}

class _EditReplyScreenState extends State<EditReplyScreen> {


  @override
  Widget build(BuildContext context) {
    PostProvider postProvider = Provider.of(context);
    return Scaffold(
      body: SingleChildScrollView(
        child: SafeArea(
          child: Padding(
            padding: const EdgeInsets.symmetric(vertical:20.0,horizontal: 10),
            child: ,
          ),
        ),
      ),
    );
  }
}
*/
